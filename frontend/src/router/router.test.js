/** Route-matrix tests protect the three distinct authenticated workspaces. */
import {describe,it,expect} from 'vitest';import router from './index';describe('ASTRA router',()=>{it('contains public and role dashboards',()=>{const paths=router.getRoutes().map(r=>r.path);expect(paths).toContain('/cars/:id');expect(paths).toContain('/client/dashboard');expect(paths).toContain('/owner/dashboard');expect(paths).toContain('/admin/dashboard')});it('does not expose role selection through registration routes',()=>{expect(router.getRoutes().some(r=>r.path.includes('register/:role'))).toBe(false)})})
